project_name: traffic_demand_prediction
seed: 42
log_level: INFO
features:
  enabled:
    - rolling_mean_3

paths:
  artifacts_dir: artifacts/experiments/rolling_only
  oof_dir: oof/experiments/rolling_only
  reports_dir: reports/experiments/rolling_only
  logs_dir: artifacts/logs/experiments/rolling_only
  submission_dir: submissions/experiments/rolling_only
  logs_dir: artifacts/logs/experiments/rolling_only