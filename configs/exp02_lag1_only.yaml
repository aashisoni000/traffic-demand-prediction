project_name: traffic_demand_prediction
seed: 42
log_level: INFO
features:
  enabled:
    - lag1_demand

paths:
  artifacts_dir: artifacts/experiments/lag1_only
  oof_dir: oof/experiments/lag1_only
  reports_dir: reports/experiments/lag1_only
  logs_dir: artifacts/logs/experiments/lag1_only
  submission_dir: submissions/experiments/lag1_only
  logs_dir: artifacts/logs/experiments/lag1_only